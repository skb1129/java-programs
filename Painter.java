import java.util.*;

/* Minimum time to paint all the Boards*/

public class Painter {
	public static void main(String[] args) {
		int painters, boards;
		Scanner scan = new Scanner(System.in);
		painters = scan.nextInt();
		boards = scan.nextInt();
		int[] times = new int[n];
		int total = 0;
		for (int i = 0; i < n; i++) {
			times[i] = scan.nextInt();
			total += times[i];
		}
		int avg = total/p;
	}
}